@page
@model GetAllAnnouncementsModel
@{
    ViewData["Title"] = "All Announcements";
}

<h1>All Announcements</h1>

@if (HttpContext.Session.GetString("User") != null)
{
    <a asp-page="/Forum/CreateAnnouncement" class="btn btn-primary mb-3">Create New Announcement</a>
}
else
{
    <p>Please <a asp-page="/User/Login">log in</a> to create announcements.</p>
}

@* ---------------- Service Announcements ---------------- *@
<h2>Service Beskeder</h2>
<table class="table table-bordered">
    <thead>
        <tr>
            <th>Title</th>
            <th>Text</th>
            <th>Upload</th>
            <th>Actual</th>
            <th>Announcer</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var announcement in Model.ServiceAnnouncements)
        {
            <tr>
                <td>@announcement.Title</td>
                <td>@announcement.Text</td>
                <td>@announcement.UploadTime.ToShortDateString()</td>
                <td>
                    @if (Model.IsAdmin)
                    {
                        <form method="post" asp-page-handler="ToggleActual">
                            <input type="hidden" name="id" value="@announcement.Id" />
                            <input type="checkbox" name="actual" value="true"
                                   onchange="this.form.submit()"
                            @(announcement.Actual == true ? "checked" : "") />
                        </form>
                    }
                    else
                    {
                        <input type="checkbox" disabled @(announcement.Actual == true ? "checked" : "") />
                    }
                </td>
                <td>@announcement.Announcer?.Name</td>
                <td>
                    @if ((announcement.Announcer?.Id == Model.CurrentUserId) || Model.IsAdmin)
                    {
                        <a asp-page="UpdateAnnouncement" asp-route-id="@announcement.Id" class="btn btn-warning">Opdater</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>


@* ---------------- Partner Announcements ---------------- *@
<h2>Søger Partner</h2>
<table class="table table-bordered">
    <thead>
        <tr>
            <th>Title</th>
            <th>Text</th>
            <th>Upload</th>
            <th>Announcer</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var announcement in Model.PartnerAnnouncements)
        {
            <tr>
                <td>@announcement.Title</td>
                <td>@announcement.Text</td>
                <td>@announcement.UploadTime.ToShortDateString()</td>
                <td>@announcement.Announcer?.Name</td>
                <td>
                    @if ((announcement.Announcer?.Id == Model.CurrentUserId) || Model.IsAdmin)
                    {
                        <a asp-page="UpdateAnnouncement" asp-route-id="@announcement.Id" class="btn btn-warning">Opdater</a>
                        <form method="post" asp-page-handler="Delete" asp-route-id="@announcement.Id" class="d-inline">
                            <button type="submit" class="btn btn-danger" onclick="return confirm('Er du sikker på, at du vil slette denne besked?');">Slet</button>
                        </form>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>

@* ---------------- General Announcements ---------------- *@
<h2>Generele Fora</h2>
<table class="table table-bordered">
    <thead>
        <tr>
            <th>Title</th>
            <th>Text</th>
            <th>Upload</th>
            <th>Announcer</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var announcement in Model.GeneralAnnouncements)
        {
            <tr>
                <td>@announcement.Title</td>
                <td>@announcement.Text</td>
                <td>@announcement.UploadTime.ToShortDateString()</td>
                <td>@announcement.Announcer?.Name</td>
                <td>
                @if ((announcement.Announcer?.Id == Model.CurrentUserId) || Model.IsAdmin)
                {
                    <a asp-page="UpdateAnnouncement" asp-route-id="@announcement.Id" class="btn btn-warning">Opdater</a>
                        <form method="post" asp-page-handler="Delete" asp-route-id="@announcement.Id" class="d-inline">
                            <button type="submit" class="btn btn-danger" onclick="return confirm('Er du sikker på, at du vil slette denne besked?');">Slet</button>
                        </form>
                }
                </td>
            </tr>
        }
    </tbody>
</table>